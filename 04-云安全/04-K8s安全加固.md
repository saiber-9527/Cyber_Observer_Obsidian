# Kubernetes 安全加固

> K8s 安全不是"加个 WAF 就完事"，需要从多层面入手。

---

## K8s 安全四层模型

```
云基础设施层
  ├── 云账号安全（IAM）
  ├── 集群网络隔离（VPC）
  └── 节点安全（EC2/ECS 加固）

K8s 控制平面
  ├── API Server 安全
  ├── etcd 加密
  ├── RBAC 权限
  └── 准入控制

K8s 工作负载
  ├── Pod 安全标准
  ├── 网络策略
  ├── Secret 管理
  └── 容器运行时安全

应用层
  ├── 代码安全
  ├── 镜像安全
  └── 配置安全
```

---

## 安全加固清单

### 1. API Server 加固

```yaml
# kube-apiserver 配置

# 禁止匿名访问
--anonymous-auth=false

# 启用 TLS
--tls-cert-file=/etc/kubernetes/pki/apiserver.crt
--tls-private-key-file=/etc/kubernetes/pki/apiserver.key

# 限制客户端证书认证（启用 Service Account 密钥自动轮转）
--service-account-key-file=/etc/kubernetes/pki/sa.pub
--service-account-signing-key-file=/etc/kubernetes/pki/sa.key
--service-account-issuer=kubernetes.default.svc

# 审计日志
--audit-log-path=/var/log/kube-apiserver-audit.log
--audit-log-maxage=30
--audit-log-maxsize=100
--audit-log-format=json
```

### 2. etcd 加密

```yaml
# etcd 存储了集群所有信息（包括 Secret 明文）
# 开启 etcd 加密

# EncryptionConfiguration
apiVersion: apiserver.config.k8s.io/v1
kind: EncryptionConfiguration
resources:
  - resources:
      - secrets
    providers:
      - aescbc:  # 使用 AES-CBC 加密
          keys:
            - name: key1
              secret: c2VjcmV0IGlzIHNlY3VyZQ==
      - identity: {}  # 作为兜底（不加密）
```

### 3. RBAC（最小权限）

```yaml
# 不要用 cluster-admin！
# 创建最小权限的角色

# 只读角色
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: default
  name: pod-reader
rules:
- apiGroups: [""]
  resources: ["pods", "pods/log"]
  verbs: ["get", "list", "watch"]
---
# 绑定给需要的用户
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: read-pods
  namespace: default
subjects:
- kind: ServiceAccount
  name: monitor-sa
roleRef:
  kind: Role
  name: pod-reader
  apiGroup: rbac.authorization.k8s.io
```

### 4. Pod 安全标准

```yaml
# Kubernetes v1.25+ 内置准入控制器

apiVersion: v1
kind: Namespace
metadata:
  name: production
  labels:
    # 强制 Pod 安全策略
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/enforce-version: latest
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
---
# Pod 级别安全设置
apiVersion: v1
kind: Pod
metadata:
  name: secure-pod
spec:
  securityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  containers:
  - name: app
    image: myapp:latest
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop: ["ALL"]
      readOnlyRootFilesystem: true
```

### 5. 网络策略

```yaml
# 默认拒绝所有入站/出站流量，再按需放行
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: production
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
---
# 允许前端访问后端
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-frontend-to-backend
  namespace: production
spec:
  podSelector:
    matchLabels:
      app: backend
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: frontend
    ports:
    - port: 8080
```

### 6. Secret 管理（重要！）

```yaml
# ❌ K8s Secret 只是 base64 编码，不是加密
# 默认存在 etcd 中明文可读

# ✅ 使用外部密钥管理系统
# 阿里云 KMS Provider
# CSI Secret Store Driver 挂载外部密钥
```

---

## 安全工具推荐

| 类别 | 工具 | 说明 |
|------|------|------|
| **配置审计** | kube-bench | CIS K8s 基准检查 |
| **漏洞扫描** | kube-hunter | 寻找 K8s 集群漏洞 |
| **策略引擎** | Kyverno / OPA Gatekeeper | 准入控制策略 |
| **运行时安全** | Falco | 容器行为检测 |
| **网络策略** | Calico / Cilium | 细粒度网络隔离 |
| **镜像扫描** | Trivy | 检查镜像漏洞 |
| **审计** | K10s / KubeAudit | 审计日志分析 |

---

## 快速安全检查（kube-bench）

```bash
# 安装 kube-bench
wget https://github.com/aquasecurity/kube-bench/releases/...
./kube-bench run --targets master

# 输出示例
[PASS] 1.1.1 Ensure that the API server pod specification file permissions are set to 644 or more restrictive
[FAIL] 1.2.1 Ensure that the --anonymous-auth argument is set to false
[INFO] 1.4.1 Ensure that the kubelet service file permissions are set to 644 or more restrictive
[WARN] 2.1.1 Ensure that the --allow-privileged argument is set to false
```

---

## ⚠️ K8s 安全最容易忽略的地方

1. **etcd 默认不加密** — 等于所有 Secret 明文存储，务必开启加密
2. **默认 Service Account 权限过大** — 每个 Pod 默认挂载 SA Token，但不是每个 Pod 都需要
3. **NetworkPolicy 不是默认开启** — 没有网络策略时，所有 Pod 可以互相访问
4. **"容器逃逸"不是传说** — 共享内核决定了容器隔离不如 VM

#云安全 #K8s安全 #容器安全 #最佳实践
